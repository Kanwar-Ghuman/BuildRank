import { SubmitClient } from "./submit-client";

export const metadata = {
  title: "Submit a Project — BuildRank",
  description: "Share your project with the BuildRank community.",
};

export default function SubmitPage() {
  return <SubmitClient />;
}
